export enum SortEnum {
  "id" = "id",
  "-id" = "-id",
  "name" = "name",
  "-name" = "-name",
  "email" = "email",
  "-email" = "-email",
  "createdAt" = "createdAt",
  "-createdAt" = "-createdAt",
}
